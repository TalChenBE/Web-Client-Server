# node.js-express server
Express runs on port `3000`. change in `src/app.js` and inside the `Dockerfile`.

This is a server project.

> npm init
> npm i -g nodemon
> and now the run of the file will be like the fllowwing:
> nodemon.cmd {file_name}.js

# Enjoy! :smiley: